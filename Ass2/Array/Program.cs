using System;

namespace Array
{
    class Program
    {
        static void Main(string[] args)
        {
            //1) To declare an array, define the variable type with square brackets: string[] cars;
            //  We have now declared a variable that holds an array of strings. To reference a particular element in an array, 
            // specify its row and column number using the following syntax, where A is the matrix variable. 
            // Always specify the row first and column second.

        //     2) You allocate memory for an array when yom want to build data structures (containers) 
        //     without a fixed upper size
        // }
    }
}
